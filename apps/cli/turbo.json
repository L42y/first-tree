{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "context-integration/**"],
      "inputs": ["$TURBO_DEFAULT$", "../../skills/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
