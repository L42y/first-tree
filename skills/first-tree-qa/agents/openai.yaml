interface:
  display_name: "First Tree QA"
  short_description: "Run risk-scoped QA with reusable environments"
  default_prompt: "Use $first-tree-qa to choose the smallest credible validation scope, reuse the QA environment safely, and report bounded evidence."
