interface:
  display_name: "Sync Meeting Records to Tree"
  short_description: "Turn meeting records into reviewed Tree changes"
  default_prompt: "Use $sync-meeting-records-to-tree to sync durable context from the meeting records I supplied into the team's Context Tree."
