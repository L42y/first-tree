interface:
  display_name: "Context Tree Audit"
  short_description: "Audit stored Context Tree decisions safely"
  default_prompt: "Use $context-tree-audit to audit the requested stored normal Context Tree scope on a stable snapshot of the actual binding branch and route evidence-backed findings safely, using the provider-appropriate independent review path for any resulting PR/MR."
